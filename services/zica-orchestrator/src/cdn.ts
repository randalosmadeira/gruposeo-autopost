import { credential } from './config.js';import type { TargetRecord } from './types.js';
export async function purgeCdn(target:TargetRecord,urls:string[]){const ref=typeof target.config.cloudflareCredentialRef==='string'?target.config.cloudflareCredentialRef:'';if(!ref||!urls.length)return{skipped:true};const creds=credential(ref);if(!creds.apiToken||!creds.zoneId)throw new Error(`Cloudflare credential incomplete: ${ref}`);const response=await fetch(`https://api.cloudflare.com/client/v4/zones/${encodeURIComponent(creds.zoneId)}/purge_cache`,{method:'POST',headers:{Authorization:`Bearer ${creds.apiToken}`,'Content-Type':'application/json'},body:JSON.stringify({files:urls.slice(0,30)}),signal:AbortSignal.timeout(15000)});if(!response.ok)throw new Error(`Cloudflare purge HTTP ${response.status}`);return{purged:Math.min(urls.length,30)};}
