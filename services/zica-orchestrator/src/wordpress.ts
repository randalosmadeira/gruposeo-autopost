import crypto from 'node:crypto';import { credential } from './config.js';import { assertPublicHttpUrl,signBody } from './security.js';import type { ArticlePayload,TargetRecord,WordPressEvent } from './types.js';
function siteBase(target:TargetRecord){const configured=typeof target.config.deliverySiteUrl==='string'?target.config.deliverySiteUrl:target.site_url;return assertPublicHttpUrl(configured).toString().replace(/\/$/,'');}
export async function fetchArticle(target:TargetRecord,event:WordPressEvent){const creds=credential(target.credential_ref);if(!creds.apiKey)throw new Error(`apiKey missing for ${target.credential_ref}`);const source=typeof target.config.sourceSiteUrl==='string'?target.config.sourceSiteUrl:event.site;const base=assertPublicHttpUrl(source).toString().replace(/\/$/,'');const response=await fetch(`${base}/wp-json/zica-posts/v1/articles/${event.post.id}`,{headers:{'X-ZICA-POSTS-Key':creds.apiKey,Accept:'application/json'},signal:AbortSignal.timeout(20000)});if(!response.ok)throw new Error(`WordPress article fetch HTTP ${response.status}`);const json=await response.json() as {success?:boolean;data?:ArticlePayload};if(!json.success||!json.data)throw new Error('WordPress article payload invalid');return json.data;}
export async function pushHubSync(target:TargetRecord,article:ArticlePayload,event:WordPressEvent){const hmacCreds=credential(target.hmac_secret_ref);if(!hmacCreds.hmacSecret)throw new Error(`hmacSecret missing for ${target.hmac_secret_ref}`);const base=siteBase(target);const body=JSON.stringify({post_id:article.id,title:article.title,content:article.content,excerpt:article.excerpt,slug:article.slug,content_hash:article.content_hash,correlation_id:event.correlation_id,schema_json:article.schema_json});const timestamp=Math.floor(Date.now()/1000).toString();const nonce=crypto.randomUUID();const response=await fetch(`${base}/wp-json/zica-posts/v1/hub/sync`,{method:'POST',headers:{'Content-Type':'application/json','X-Zica-Timestamp':timestamp,'X-Zica-Nonce':nonce,'X-Zica-Signature':signBody(body,hmacCreds.hmacSecret,timestamp,nonce),'X-Zica-Correlation-ID':event.correlation_id},body,signal:AbortSignal.timeout(30000)});if(!response.ok)throw new Error(`WordPress hub sync HTTP ${response.status}`);return response.json() as Promise<Record<string,unknown>>;}
export async function triggerReconcile(target:TargetRecord){const creds=credential(target.credential_ref);if(!creds.apiKey)throw new Error(`apiKey missing for ${target.credential_ref}`);const base=siteBase(target);const response=await fetch(`${base}/wp-json/zica-posts/v1/sync`,{method:'POST',headers:{'X-ZICA-POSTS-Key':creds.apiKey,'Content-Type':'application/json'},body:'{}',signal:AbortSignal.timeout(45000)});if(!response.ok)throw new Error(`WordPress reconcile HTTP ${response.status}`);return response.json() as Promise<Record<string,unknown>>;}
